#include <stdio.h>
#include <stdlib.h>

void insertsort(int a[], int n)
{
    
}

int main()
{
    int arr[] = {1,6,2,8,3,9,4};
    int n = sizeof(arr)/sizeof(int);
    insertsort(arr, n);
    return 0;
}